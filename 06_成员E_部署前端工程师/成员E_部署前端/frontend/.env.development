# 后端 API 地址(开发环境)
# 留空 = 使用相对路径 /api → 由 Vite 开发服务器代理到本机 8000
#   好处: 局域网内其他电脑访问 http://<本机IP>:5173 时, 也能正常拿到分类结果
# 若不想走代理(直接连后端): 改成 http://localhost:8000 (仅本机可用, 局域网不行)
VITE_API_BASE_URL=
# 当后端不可用且开启演示模式时, 前端展示"演示数据"(非真实模型结果)
VITE_USE_MOCK=false
